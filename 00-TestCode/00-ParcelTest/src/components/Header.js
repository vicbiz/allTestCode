const Header = () => {
    const template = `
        <header>
            <h1>My Parcel App</h1>
            <p>This is boilerplate for a simple JS workflow using Parcel</p>
        </header>
    `;
    return template;
}

export default Header;